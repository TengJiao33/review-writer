interface:
  display_name: "Topic Paper Discovery"
  short_description: "Plan, retrieve, and screen review evidence across domains."
  default_prompt: "Use $review-topic-paper-discovery to derive queries from the topic contract, search local and external sources, and record screening decisions."
